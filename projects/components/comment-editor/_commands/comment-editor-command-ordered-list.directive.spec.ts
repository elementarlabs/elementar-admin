import { CommentEditorCommandOrderedListDirective } from './comment-editor-command-ordered-list.directive';

describe('CommentEditorCommandOrderedListDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandOrderedListDirective();
    expect(directive).toBeTruthy();
  });
});
