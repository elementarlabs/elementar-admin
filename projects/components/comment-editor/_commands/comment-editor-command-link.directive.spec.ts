import { CommentEditorCommandLinkDirective } from './comment-editor-command-link.directive';

describe('CommentEditorCommandLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandLinkDirective();
    expect(directive).toBeTruthy();
  });
});
