import { SidebarFullViewModeDirective } from './sidebar-full-view-mode.directive';

describe('SidebarFullViewModeDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarFullViewModeDirective();
    expect(directive).toBeTruthy();
  });
});
