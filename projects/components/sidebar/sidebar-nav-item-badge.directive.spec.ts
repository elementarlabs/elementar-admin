import { SidebarNavItemBadgeDirective } from './sidebar-nav-item-badge.directive';

describe('SidebarNavItemBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarNavItemBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
