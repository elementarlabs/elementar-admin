import { SidebarNavGroupToggleIconDirective } from './sidebar-nav-group-toggle-icon.directive';

describe('SidebarNavGroupToggleIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarNavGroupToggleIconDirective();
    expect(directive).toBeTruthy();
  });
});
