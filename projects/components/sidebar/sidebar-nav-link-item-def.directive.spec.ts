import { SidebarNavLinkItemDefDirective } from './sidebar-nav-link-item-def.directive';

describe('SidebarNavLinkItemDefDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarNavLinkItemDefDirective();
    expect(directive).toBeTruthy();
  });
});
