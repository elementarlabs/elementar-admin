import { SidebarNavGroupDefDirective } from './sidebar-nav-group-def.directive';

describe('SidebarNavGroupDefDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarNavGroupDefDirective();
    expect(directive).toBeTruthy();
  });
});
