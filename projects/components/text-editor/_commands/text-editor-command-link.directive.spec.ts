import { TextEditorCommandLinkDirective } from './text-editor-command-link.directive';

describe('TextEditorCommandLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandLinkDirective();
    expect(directive).toBeTruthy();
  });
});
