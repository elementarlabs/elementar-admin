import { TextEditorCommandBoldDirective } from './text-editor-command-bold.directive';

describe('TextEditorCommandBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandBoldDirective();
    expect(directive).toBeTruthy();
  });
});
