import { TextEditorCommandCodeDirective } from './text-editor-command-code.directive';

describe('TextEditorCommandCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandCodeDirective();
    expect(directive).toBeTruthy();
  });
});
