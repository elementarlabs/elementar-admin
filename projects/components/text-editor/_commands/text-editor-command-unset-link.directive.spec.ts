import { TextEditorCommandUnsetLinkDirective } from './text-editor-command-unset-link.directive';

describe('TextEditorCommandUnsetLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandUnsetLinkDirective();
    expect(directive).toBeTruthy();
  });
});
