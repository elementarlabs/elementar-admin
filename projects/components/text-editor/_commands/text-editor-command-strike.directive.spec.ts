import { TextEditorCommandStrikeDirective } from './text-editor-command-strike.directive';

describe('TextEditorCommandStrikeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandStrikeDirective();
    expect(directive).toBeTruthy();
  });
});
