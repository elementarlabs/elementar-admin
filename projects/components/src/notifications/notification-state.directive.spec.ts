import { NotificationStateDirective } from './notification-state.directive';

describe('NotificationStateDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationStateDirective();
    expect(directive).toBeTruthy();
  });
});
