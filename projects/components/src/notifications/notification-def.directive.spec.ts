import { NotificationDefDirective } from './notification-def.directive';

describe('NotificationDefDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationDefDirective();
    expect(directive).toBeTruthy();
  });
});
