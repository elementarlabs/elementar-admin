import { DashboardWidgetDefDirective } from './dashboard-widget-def.directive';

describe('DashboardWidgetDefDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardWidgetDefDirective();
    expect(directive).toBeTruthy();
  });
});
