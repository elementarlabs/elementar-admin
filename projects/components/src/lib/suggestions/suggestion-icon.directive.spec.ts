import { SuggestionIconDirective } from './suggestion-icon.directive';

describe('SuggestionIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestionIconDirective();
    expect(directive).toBeTruthy();
  });
});
