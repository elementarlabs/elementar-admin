import { CarouselControlsDirective } from './carousel-controls.directive';

describe('CarouselControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselControlsDirective();
    expect(directive).toBeTruthy();
  });
});
