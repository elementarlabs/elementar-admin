import { SoundEffectDirective } from './sound-effect.directive';

describe('SoundEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new SoundEffectDirective();
    expect(directive).toBeTruthy();
  });
});
