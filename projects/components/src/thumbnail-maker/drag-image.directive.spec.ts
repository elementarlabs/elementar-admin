import { DragImageDirective } from './drag-image.directive';

describe('DragImageDirective', () => {
  it('should create an instance', () => {
    const directive = new DragImageDirective();
    expect(directive).toBeTruthy();
  });
});
