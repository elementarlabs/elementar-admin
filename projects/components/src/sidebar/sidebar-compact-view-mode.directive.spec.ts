import { SidebarCompactViewModeDirective } from './sidebar-compact-view-mode.directive';

describe('SidebarCompactViewModeDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarCompactViewModeDirective();
    expect(directive).toBeTruthy();
  });
});
