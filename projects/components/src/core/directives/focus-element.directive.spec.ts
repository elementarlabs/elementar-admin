import { FocusElementDirective } from './focus-element.directive';

describe('FocusElementDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusElementDirective();
    expect(directive).toBeTruthy();
  });
});
