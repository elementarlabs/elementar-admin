import { CommentEditorFooterBarContentDirective } from './comment-editor-footer-bar-content.directive';

describe('CommentEditorFooterBarContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorFooterBarContentDirective();
    expect(directive).toBeTruthy();
  });
});
