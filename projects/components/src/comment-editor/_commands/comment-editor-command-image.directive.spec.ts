import { CommentEditorCommandImageDirective } from './comment-editor-command-image.directive';

describe('CommentEditorCommandImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandImageDirective();
    expect(directive).toBeTruthy();
  });
});
