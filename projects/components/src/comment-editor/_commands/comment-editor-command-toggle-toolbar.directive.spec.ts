import { CommentEditorCommandToggleToolbarDirective } from './comment-editor-command-toggle-toolbar.directive';

describe('CommentEditorCommandToggleToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandToggleToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
