import { CommentEditorCommandBlockquoteDirective } from './comment-editor-command-blockquote.directive';

describe('CommentEditorCommandBlockquoteDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandBlockquoteDirective();
    expect(directive).toBeTruthy();
  });
});
