import { CommentEditorCommandDirective } from './comment-editor-command.directive';

describe('CommentEditorCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditorCommandDirective();
    expect(directive).toBeTruthy();
  });
});
