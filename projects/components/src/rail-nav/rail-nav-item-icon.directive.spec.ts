import { RailNavItemIconDirective } from './rail-nav-item-icon.directive';

describe('RailNavItemIconDirective', () => {
  it('should create an instance', () => {
    const directive = new RailNavItemIconDirective();
    expect(directive).toBeTruthy();
  });
});
