import { CarouselPreviousDirective } from './carousel-previous.directive';

describe('CarouselPreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselPreviousDirective();
    expect(directive).toBeTruthy();
  });
});
