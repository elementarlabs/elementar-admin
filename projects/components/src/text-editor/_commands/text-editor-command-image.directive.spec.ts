import { TextEditorCommandImageDirective } from './text-editor-command-image.directive';

describe('TextEditorCommandImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandImageDirective();
    expect(directive).toBeTruthy();
  });
});
