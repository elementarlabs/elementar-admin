import { TextEditorCommandBulletListDirective } from './text-editor-command-bullet-list.directive';

describe('TextEditorCommandBulletListDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandBulletListDirective();
    expect(directive).toBeTruthy();
  });
});
