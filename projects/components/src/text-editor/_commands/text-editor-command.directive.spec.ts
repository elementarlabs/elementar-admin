import { TextEditorCommandDirective } from './text-editor-command.directive';

describe('TextEditorCommandDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandDirective();
    expect(directive).toBeTruthy();
  });
});
