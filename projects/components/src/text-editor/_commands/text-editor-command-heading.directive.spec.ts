import { TextEditorCommandHeadingDirective } from './text-editor-command-heading.directive';

describe('TextEditorCommandHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditorCommandHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
