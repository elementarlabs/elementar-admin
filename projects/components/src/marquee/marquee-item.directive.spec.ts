import { MarqueeItemDirective } from './marquee-item.directive';

describe('MarqueeItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MarqueeItemDirective();
    expect(directive).toBeTruthy();
  });
});
