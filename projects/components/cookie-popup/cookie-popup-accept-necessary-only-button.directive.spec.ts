import { CookiePopupAcceptNecessaryOnlyButtonDirective } from './cookie-popup-accept-necessary-only-button.directive';

describe('CookiePopupAcceptNecessaryOnlyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CookiePopupAcceptNecessaryOnlyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
