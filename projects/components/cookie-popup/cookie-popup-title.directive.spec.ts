import { CookiePopupTitleDirective } from './cookie-popup-title.directive';

describe('CookiePopupTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CookiePopupTitleDirective();
    expect(directive).toBeTruthy();
  });
});
