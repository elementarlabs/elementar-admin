import { KanbanItemDefDirective } from './kanban-item-def.directive';

describe('KanbanItemDefDirective', () => {
  it('should create an instance', () => {
    const directive = new KanbanItemDefDirective();
    expect(directive).toBeTruthy();
  });
});
