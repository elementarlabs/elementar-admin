import { MenuOptionGroupDirective } from './menu-option-group.directive';

describe('MenuOptionGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuOptionGroupDirective();
    expect(directive).toBeTruthy();
  });
});
