import { SuggestionThumbDirective } from './suggestion-thumb.directive';

describe('SuggestionThumbDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestionThumbDirective();
    expect(directive).toBeTruthy();
  });
});
