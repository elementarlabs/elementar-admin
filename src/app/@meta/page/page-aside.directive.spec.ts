import { PageAsideDirective } from './page-aside.directive';

describe('PageAsideDirective', () => {
  it('should create an instance', () => {
    const directive = new PageAsideDirective();
    expect(directive).toBeTruthy();
  });
});
