import { PageContentDirective } from './page-content.directive';

describe('PageContentDirective', () => {
  it('should create an instance', () => {
    const directive = new PageContentDirective();
    expect(directive).toBeTruthy();
  });
});
